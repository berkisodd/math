# Repo notes for Claude

- When Claude opens a pull request in this repo, merge it into `main` right away (squash merge) instead of waiting for the user to say "merge to main" — the user has asked for this as a standing preference.
